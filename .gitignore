# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / editor cruft
.DS_Store
*.swp
*~

# Internal session logs / research harnesses (not for publication)
internal/

# local artifacts (wheels kept out of git)
patch/*.whl

# bench: smoke-test results are transient (real releases are committed)
bench/results/smoke/
