# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / editor cruft
.DS_Store
*.swp
*~

# Internal session logs / research harnesses (not for publication)
internal/

# Full evaluation receipts include host/container topology. Only explicitly
# sanitized artifacts under evidence/public are publishable.
evidence/*
!evidence/public/
!evidence/public/**

# Deployment-specific guarded controllers/provenance contain private topology.
tools/capture_w2_eval_provenance.sh
tools/guarded_w2_eval_remote.sh
tools/guarded_w2_restage_remote.sh
tools/ds4_eval/tests/test_controller_guards.py

# local artifacts (wheels kept out of git)
patch/*.whl

# bench: smoke-test results are transient (real releases are committed)
bench/results/smoke/
